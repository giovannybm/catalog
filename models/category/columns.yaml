# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    category_name:
        label: Name
        searchable: true
    image:
      label: Image
      type: fileupload
      mode: image
